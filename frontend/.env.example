# Local dev: Django API base URL. Leave unset to default to http://localhost:8000/api
# Do not point production (e.g. Vercel) builds at Railway here — use same-origin /api + vercel.json rewrite.
VITE_API_URL=http://localhost:8000/api
